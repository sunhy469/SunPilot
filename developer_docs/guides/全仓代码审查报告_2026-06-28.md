# SunPilot 全仓代码审查报告

审查日期：2026-06-28

代码基线：`main` / `6ca9596`

审查范围：`packages/*/src`、`tests/integration`、根级工程配置、PostgreSQL migrations、前端生产构建及生产依赖树。

审查约束：本次只审查与写文档，没有修改任何产品代码。

## 1. 结论先行

SunPilot 已经具备较完整的 Agent Runtime 骨架：包边界清楚，Agent Loop、内容块流式输出、工具调用、审批、PostgreSQL 持久化、记忆、追踪、Web Chat 与 Digital World 都不是空壳。但当前版本还不能按“生产安全、行为闭环、门禁可信”验收。

本轮确认的最高优先级问题不是代码风格，而是以下真实运行风险：

1. `PromptInjectionDetector`、`ToolSandbox`、`TaskScopedPermissionManager` 虽已实例化，却没有进入实际工具执行判定；
2. 第三方 Skill 仍通过动态 `import()` 在 daemon 进程内执行，可直接绕过 SDK 权限接口；
3. Digital World 的异步任务续跑、字段清空和 artifact 桥接存在多处确定性错误；
4. 迁移目录已有 `022`，但迁移执行器只运行到 `021`，新数据库与当前开发库会产生结构分叉；
5. 全仓类型检查失败，但 `pnpm build` 仍会成功，当前构建门禁会放过前端类型错误；
6. 测试会继承真实 Seed 密钥并访问外部模型，造成失败、成本和数据外发风险。

审查结果按严重度汇总：

| 严重度 | 数量 | 含义 |
| --- | ---: | --- |
| P0 | 2 | 安全边界失效，发布前必须处理 |
| P1 | 14 | 核心功能、数据一致性或可靠性缺陷，应进入最近修复批次 |
| P2 | 9 | 中期工程治理、性能和契约问题 |

### 1.1 2026-06-29 第一优先级修复进展

本节记录审查后的代码修复，不改写 2026-06-28 的原始问题证据。当前已完成 6 个问题和 1 个问题的测试隔离子项：

| 问题 | 状态 | 修复结果 |
| --- | --- | --- |
| P0-01 工具安全模块未进入执行路径 | 已修复 | 新增强制 `ToolSafetyBoundary`，native、审批续跑和直接 orchestrator 共用 sandbox、作用域授权和 prompt injection 前后置校验 |
| P1-01 类型检查与构建门禁 | 已修复 | 修复 Web 的 3 个 TypeScript 错误，`@sunpilot/web` 构建改为先执行 `tsc --noEmit` |
| P1-03 测试继承真实模型密钥 | 部分修复 | fake provider 注入时禁用环境 Seed/embedding provider；DP 专用环境变量和默认模型选择仍待收敛 |
| P1-03 剩余：移除 `createDefaultLlmProvider()` | 已明确方案 | `createDefaultLlmProvider()` 只认 `SUNPILOT_LLM_API_KEY`，不识别 `SUNPILOT_DP_LLM_API_KEY`；方案：删除该冗余函数，全量收口到 `composition-root.ts` 自建 provider（详见 P1-03 节） |
| P1-04 fast-ack 失败清理 | 已修复 | 图片校验提前到幂等预留之前；准备失败释放幂等键、Abort controller 和事件订阅，并允许同请求重试 |
| P1-05 assistant 持久化假成功 | 已修复 | embedding 仍可 best-effort，但消息主体写入失败会向上抛出，不再发送 `agent.message.completed` |
| P1-06 动态风险与多工具审批 | 已修复 | native function-calling 返回结构化审批交接；整个工具批次只创建一个持久化 approval，批准后按快照执行完整批次 |
| P1-11 migration 022/023 漂移 | 已修复 | 迁移改为目录自动发现，修正 022 外键 SQL，补回 023 Agent Run 索引，并通过真实空库 smoke test |

修复后验证：`pnpm build`、`pnpm typecheck`、`pnpm test` 均通过；全仓共 606 个测试通过。`pnpm lint` 为 0 个错误、6 个既有 React Hooks 警告。真实空 PostgreSQL 已顺序执行 23 个 migration，并确认 12 个 Digital World 外键及 `idx_world_actions_agent_run` 索引存在。

## 2. 审查方法与覆盖面

本次不是只抽查 Agent Loop，而是采用以下组合方式：

- 盘点 10 个运行包、321 个非测试 TypeScript/TSX 源文件、60 个测试文件、22 个 SQL migration；
- 阅读 daemon 装配根、HTTP/WS 入口、AgentService、AgentLoop、native tool loop、安全策略、SkillRunner、PostgreSQL repositories、Digital World 与 Web 状态链路；
- 对事件、模型、权限、附件、审批、任务、artifact、memory、迁移和 CLI 进程控制做跨包追踪；
- 执行 `typecheck`、`lint`、`test`、`build` 和生产依赖安全审计；
- 只读查询本机 PostgreSQL 的 migration/constraint 实况，用于识别“当前开发库可用但新安装会缺失”的问题；
- 对每个高优先级问题同时核对生产代码路径与现有测试覆盖，避免把旧文档结论当作当前事实。

代码规模约 71,236 行 TypeScript/TSX，其中 `core` 约 35,024 行、`web` 约 18,635 行、`storage` 约 6,202 行、`daemon` 约 5,314 行。

## 3. P0 问题

### P0-01：三个安全模块已装配但没有执行（已修复）

修复结果（2026-06-29）：

- 新增 `ToolSafetyBoundary`，并将其设为 `ExecutionOrchestrator` 的必需依赖；执行前统一校验不可信参数注入、filesystem/shell/network sandbox 和 task-scoped permission，执行后在工具结果进入模型上下文前扫描 prompt injection；
- `composition-root.ts` 现在将 `PromptInjectionDetector`、`ToolSandbox`、`TaskScopedPermissionManager` 注入该边界，native function calling、审批续跑和直接 orchestrator 调用均无法绕过；
- 审批续跑按持久化快照绑定精确的 tool call ID、skill ID 和参数；同 run 内参数或风险等级变化会要求重新授权，高/极高风险在非 `full` 模式下需要本次明确审批；
- 安全拒绝发生在 `agent.tool.started` 和 Step 创建之前；拒绝或检测会产生 `agent.safety.sandbox_denied`、`agent.safety.scope_reauth_required`、`agent.safety.injection_detected` 事件，生产装配测试已确认事件可持久化；
- 回归测试覆盖 strict 模式下的越界文件、危险 shell、localhost 网络请求、未授权/参数变化/风险变化，以及注入型工具结果。

证据：

- `packages/daemon/src/composition-root.ts:719-739` 创建 `PromptInjectionDetector`、`ToolSandbox`、`TaskScopedPermissionManager`；
- `packages/daemon/src/composition-root.ts:750-772` 只把它们放入 `AgentLoopEngine` 依赖；
- `packages/core/src/agent-kernel/agent-loop-engine.ts` 中 `toolSandbox` 和 `scopedPermissionManager` 没有校验调用，`grantsByRun` 只被清理；
- 唯一的 `injectionDetector.detect()` 位于 native tool loop，但 `ToolDecisionEngine` 在 `composition-root.ts:676-689` 创建时没有收到 `injectionDetector`，所以该分支在生产装配下始终不运行；
- `agent.safety.injection_detected`、`agent.safety.sandbox_denied`、`agent.safety.scope_reauth_required` 有协议类型，但当前主路径不会产生对应防护结果。

实际影响：

- `SUNPILOT_SANDBOX_MODE=strict` 只改变启动日志，不改变工具执行结果；
- 工具返回内容不会按当前设计做 prompt injection 阻断；
- 参数变化、同 run 授权范围和高风险重新确认没有被 task-scoped manager 强制执行；
- 现有架构文档把这些能力描述为“运行路径已接入”，与源码事实不一致。

建议：

1. 把安全检查放到唯一、不可绕过的 execution boundary；
2. 让 native tool call、审批续跑、传统 orchestrator 三条路径复用同一 `SafetyDecision`；
3. 先校验 sandbox/注入/参数作用域，再创建 tool started 事件和执行 step；
4. 对每个安全模块增加“装配测试 + 拒绝测试”，不能只测类本身。

验收标准：strict 模式下构造越界文件、危险 shell、未允许网络和注入型工具结果，均应被阻断并产生持久化安全事件。

### P0-02：第三方 Skill 可在 daemon 进程内任意执行代码

证据：

- `packages/skill-runner/src/runner.ts:148` 直接动态导入 Skill entry；
- 模块顶层代码在 manifest、capability 和 SDK handler 校验之前已经执行；
- Node 模块可以自行导入 `node:fs`、`node:child_process`、读取 `process.env` 或直接调用 `fetch`，无需经过 `ctx.files/http/secrets`；
- runner 的超时是 `Promise.race()`（`runner.ts:330-351`），Skill 若忽略 `AbortSignal`，超时后仍可在后台继续运行。

实际影响：

- manifest allowlist 只是对守规矩 Skill 的 SDK 约束，不是安全沙箱；
- 任意已安装/被替换的 Skill 等价于 daemon 进程权限代码；
- 超时后释放并发槽位并不能终止恶意或失控代码，可能形成资源泄漏和并发失控。

建议：

1. 不可信 Skill 必须迁移到 child process、Worker 隔离或容器；
2. 主进程仅通过结构化 IPC 提供受控文件、网络、secret 和 artifact API；
3. 超时后终止执行单元，而不是只 abort 协作信号；
4. 增加包 hash/签名/来源锁定，处理 symlink 逃逸与重定向后 SSRF；
5. 在隔离完成前，把 Skill 明确标记为“本机可信代码插件”。

## 4. P1 问题

### P1-01：全仓类型检查失败，生产构建却为绿色（已修复）

修复结果（2026-06-29）：

- `chat-state.ts` 的 Rich Card 类型路径已修正，`useChat.ts` 已显式导入 `ChatSendAckResult`；
- `packages/web/package.json` 的 `build` 已改为 `tsc --noEmit && vite build`；
- 当前 `pnpm typecheck` 与 `pnpm build` 均通过。

执行 `pnpm typecheck` 得到 3 个错误：

1. `packages/web/src/pages/ChatPage/hooks/chat/chat-state.ts:324-325` 的 `../../../rich-cards/types` 相对路径少了一层；
2. `packages/web/src/pages/ChatPage/hooks/useChat.ts:528` 使用了未导入的 `ChatSendAckResult`。

`packages/web` 的 `build` 仅运行 `vite build`，esbuild 会擦除这些类型引用，因此根级 `pnpm build` 仍成功。这会让 CI/发布误判“编译通过”。

建议：把 `tsc --noEmit` 作为 `packages/web` build 前置步骤，并在根 CI 中强制执行 `typecheck && lint && test && build`。

### P1-02：附件-only 消息在 UI 可发送，但协议层必然拒绝

证据：

- `packages/web/src/pages/ChatPage/hooks/useChat.ts:819-824` 明确允许“文本为空但有附件”；
- `packages/protocol/src/agent-commands.ts:13` 要求 `message` 至少 1 个字符；
- HTTP `parseAgentChatRequest()` 同样拒绝空文本。

影响：用户只上传图片并发送时，UI 先插入 optimistic 用户消息和 assistant placeholder，随后收到 JSON-RPC invalid params；图片理解/搜图的自然入口不可用。

建议：统一成“`message.trim()` 非空或 `attachments.length > 0` 至少满足一个”的 schema，并为 WS、REST、AgentService、UI 添加同一组契约测试。

### P1-03：模型配置、默认路由和测试环境互相冲突（部分修复）

修复结果（2026-06-29）：依赖注入 fake `llmProvider` 时，composition root 默认不再读取宿主机 Seed 和 embedding provider；daemon 也支持显式注入 `llmProvider`。带着当前宿主机真实密钥运行全仓测试时，测试 route 仍保持 `Seed available=false`，没有访问外部模型。

剩余问题：本节第 1、2 项尚未修复，即 `createDefaultLlmProvider()` 对 DP 专用环境变量的识别，以及 API/Web 默认模型从实际可用 route 推导。

该问题包含三个相互放大的子问题：

1. `createDefaultLlmProvider()` 只认 `SUNPILOT_LLM_API_KEY` / `DEEPSEEK_API_KEY`，而 composition root 还定义 `SUNPILOT_DP_LLM_API_KEY`；只配 DP 专用 key 仍在 agent 初始化前失败；
2. `/v1/models` 和 Web fallback 都固定默认 `seed`，即使 Seed `available: false`，常规单 DP 部署会默认选择不可用模型；
3. composition root 只要从环境读到 Seed key，就把 Seed 设为优先级 0。即使测试显式注入 fake `llmProvider`，未显式 `modelId` 的调用仍会访问真实 Seed。

本轮实测：

- 直接 `pnpm test` 时，`composition-root.test.ts` 访问了真实 Seed provider，并有 3 个 daemon 用例失败；
- 使用 `env -u SUNPILOT_SEED_LLM_API_KEY pnpm test` 后，全套测试通过。

影响：测试不具备 hermetic 性，会产生外部费用、发送测试 prompt/上下文，并因真实模型输出变化而随机失败。

建议：

- 建立唯一 `resolveModelConfig()`；
- API 默认值必须从“实际可用 route”推导；
- 测试装配显式禁用环境 provider，或依赖注入一旦存在就完全覆盖环境；
- CI 清空全部模型/OSS secret，并用 fake provider；真实模型 eval 放到独立、显式任务。

**子问题 1 修复方案（2026-06-29 确定，待实施）：移除 `createDefaultLlmProvider()`**

`createDefaultLlmProvider()` 当前只在 `server.ts:224` 被调用，作为 `options.llmProvider` 不存在时的 fallback。但 `composition-root.ts` L223 内部已经会自己从环境变量创建 provider（`deps.llmProvider ?? new OpenAICompatibleChatProvider({...})`），且 fallback 链完整：`SUNPILOT_DP_LLM_API_KEY → SUNPILOT_LLM_API_KEY → ""`。该函数是冗余中间层，且 env 变量识别范围比 composition-root 窄。

此外 `composition-root.ts` L159 `llm: deps.llmProvider` 传给 `LlmEmbeddingService` 是死代码——构造函数接受 `llm` 参数但从不读取 `this.deps.llm`。

修改内容：

| 文件 | 改动 |
|------|------|
| `daemon/src/composition-root.ts` | `llmProvider` 改为 optional；删除 L159 死代码 `llm: deps.llmProvider` |
| `daemon/src/server.ts` | 删除 `createDefaultLlmProvider` 的 import 和调用，改为直接传 `options.llmProvider` |
| `core/src/llm/openai-compatible.provider.ts` | 删除 `createDefaultLlmProvider` 函数定义 |
| `core/src/agent-kernel/context/llm-embedding-service.ts` | 删除构造函数中从未使用的 `llm: LlmProvider` 字段及 import |
| `core/src/llm.test.ts` | 删除 3 个相关测试用例 |

验证：`pnpm typecheck && pnpm build && pnpm test`

### P1-04：WebSocket fast-ack 的同步准备阶段缺少失败清理（已修复）

修复结果（2026-06-29）：附件引用校验已移动到幂等预留之前；conversation/run/message 准备阶段由统一 `try/catch` 保护，失败时清理 Abort controller、两个事件订阅和已创建的 Run 状态，并通过新增的 `idempotency.release()` 释放仍处于 `processing` 的预留。回归测试覆盖无效图片不产生预留、消息写入失败后资源清空、相同 `clientRequestId` 可以重试。

`AgentService.startChatCommand()` 在 `queueMicrotask()` 之前依次保留幂等键、创建 abort controller、订阅两个 event bus、创建 conversation/run/message。只有进入后台任务后才有清理逻辑。

确定性问题：

- 图片引用校验发生在幂等键 reserve 之后；校验抛错会留下最长 24 小时的 `processing` 记录；
- conversation/run/message 任一步失败会泄漏 abort entry 和订阅；
- retry 使用同一 `clientRequestId` 会得到 idempotency conflict，而不是正常重试。

建议：把所有 fast-ack 准备步骤放进 `try/catch/finally`；校验必须早于 reserve；失败时回滚/标记幂等失败并撤销订阅和 abort entry。

### P1-05：assistant 消息写库失败会被静默当成成功（已修复）

修复结果（2026-06-29）：daemon 的 `saveMessage()` 只把 embedding 生成保留为 best-effort，消息主体写入异常会继续向上抛出；`AssistantMessageStream.complete()` 在持久化失败时不再发送 `agent.message.completed`，由 Agent Loop 将 Run 转入失败路径。新增回归测试验证持久化失败不会出现消息完成事件。

证据：

- `packages/daemon/src/composition-root.ts:157-172` 的 `saveMessage()` 捕获并吞掉所有数据库异常；
- `AssistantMessageStream.complete()` 自身也把 save 失败降级为日志，然后继续发 `agent.message.completed`；
- run 随后可被标记为 `completed`。

影响：前端看到完整回复和成功 run，但刷新后 assistant 消息消失；事件、run 与 message 不再满足持久化一致性。

建议：embedding 失败可以 best-effort，消息主体写入失败不能吞掉。应重试、标记 run failed/interrupted，或将最终消息与 run completion 放入同一事务/outbox。

### P1-06：审批路径在动态风险和多工具场景下不闭环（已修复）

修复结果（2026-06-29）：

- streaming tool executor 在权限策略要求审批时返回 `approvalRequired` 结构，不执行批次中的任何工具；
- Agent Loop 将该结构交给统一 `ApprovalFlowCoordinator`，持久化 Run 的 `waiting_approval` 状态；
- 多工具由一个 approval 明确覆盖整个已验证批次，工具调用快照保存在 task state，批准后确定性执行全部调用；
- 新增动态风险不得提前执行工具、批次只能创建一个 pending approval 的回归测试。

两个问题：

- native executor 在参数生成后调用 `PermissionPolicy`。如果这时才判定需要审批，`streaming-tool-call-executor.ts:348-370` 只返回“需要审批”的失败文本，不创建 approval，也不把 run 置为 `waiting_approval`；
- 初始 decision 含多个高风险 tool call 时，`approval-flow.ts:104-120` 为每个 tool 单独创建 approval，但 `approve()` / `resumeApprovedTool()` 每次只执行一个 requested action 并可能完成整个 run。剩余 approval 会指向已完成 run。

建议：动态风险结果必须返回结构化 `require_approval`，由统一 approval coordinator 持久化；多工具应采用批次审批或维护明确的 pending tool queue，并在全部处理后才结束 run。

### P1-07：Digital Being 的“清空字段”全部是 no-op

业务代码大量使用以下 patch：

- `currentTaskId: undefined`
- `currentActionId: undefined`
- `targetNodeId: undefined`
- `sleepReason: undefined`

但 `PostgresDigitalBeingRepository.update()` 只更新 `value !== undefined` 的字段。因此任务结束、移动完成、唤醒和失败恢复都不会把数据库列设为 `NULL`。

影响：Digital Being 会长期残留旧 task/action/target/sleepReason，UI 和后续调度读取到伪状态。内存实现若用对象覆盖可能没有同样行为，进一步扩大测试与生产差异。

建议：patch 类型引入显式 `null` 清空语义，并在 repository 中将 `null` 写入 SQL；为每个生命周期动作做 PostgreSQL 集成测试。

### P1-08：Digital World 异步任务续跑会越过第二个 Agent Run

首次 `executeTask()` 遇到 `waiting_agent` 会停止后续 action，这是正确的。但 `onAgentRunCompleted()` 恢复剩余 actions 时调用 `executeAction()` 后忽略返回值。

对 `make_and_publish_video`：

1. 第一个 `work_on:make_video` 等待 Agent；
2. 完成回调继续执行到第二个 `work_on:publish_to_tiktok`，启动新的 Agent Run；
3. 回调不再暂停，继续 move home、sleep，并把 task 标记 completed；
4. 第二个 Agent Run 仍在后台运行。

建议：恢复循环和初始循环共用同一个状态机；遇到任意后续 `waiting_agent` 都必须立即返回，并持久化恢复游标。

### P1-09：Agent artifact 到 Digital World artifact 的 payload 形状不一致

Agent Loop 的 `agent.run.completed.payload.artifacts` 当前发送 artifact ID 字符串数组；daemon 的 Digital World bridge 却把每个元素按 `{type,title,name,uri}` 对象读取。

影响：创建出的 WorldArtifact 会退化为 `type: "unknown"`、`title: "产物"`、无 URI，真实产物信息丢失。

建议：协议明确 payload 是 ID 数组还是对象数组。若是 ID，应在 bridge 中通过 artifact repository 批量加载；不要对字符串做对象强制断言。

### P1-10：Digital World 的任务 claim 并不原子

`claimTask()` 虽在 transaction 中执行，但仍是普通 `SELECT` 后 `UPDATE`，没有 `SELECT ... FOR UPDATE`，也没有条件更新 `UPDATE ... WHERE status='queued' RETURNING ...`。

两个并发事务可同时读取 `queued`，随后都改为 `running` 并重复创建/执行 actions。

建议：使用单条条件更新，或在事务内行锁；为并发 claim 增加真实 PostgreSQL 测试。

### P1-11：迁移执行器漏掉仓库中的 022 migration（已修复）

修复结果（2026-06-29）：

- `postgres.migrations.ts` 不再维护容易漂移的硬编码白名单，改为扫描并校验连续的三位数字 migration；
- `022_digital_world_foreign_keys.sql` 改为通过 `pg_constraint` 判断后创建约束，避免不兼容的 `ADD CONSTRAINT IF NOT EXISTS`；
- 新增 `023_world_actions_agent_run_index.sql`，恢复 `idx_world_actions_agent_run`；
- 一次性空 PostgreSQL 实测得到 `migrationCount=23`、`foreignKeyCount=12`，并确认 023 和索引均存在。

`packages/storage/src/migrations/022_digital_world_foreign_keys.sql` 已存在，但 `postgres.migrations.ts:7-29` 的白名单只到 `021_memory_quality_relations.sql`。

本机数据库只读检查显示已经登记 `022` 和历史 `023`，所以当前开发库有相关外键/索引；但干净数据库按当前代码初始化时不会执行 022。这是典型的“老库正常、新安装缺约束”。仓库中也没有当前数据库已登记的 023 文件。

建议：迁移清单与文件目录建立自动一致性校验；禁止数据库出现仓库不存在的版本；在空数据库上执行完整 migration smoke test 并校验关键 FK/index。

### P1-12：CLI 可能因陈旧 PID 文件终止无关进程

`packages/launcher/src/index.ts:138-169` 读取 pid file 后直接 `SIGTERM`，不验证该 PID 是否仍属于 SunPilot；PID 被系统复用时会终止无关进程。fallback 又按命令行 substring 扫描，仍可能误匹配。

建议：PID 文件同时记录进程启动时间和随机实例 token；停止前比对 `/proc/<pid>/cmdline`、启动时间及 daemon health identity；匹配失败只删除 stale pid，不发送信号。

### P1-13：Memory hybrid search 会让 NULL embedding 排到最前，并限制语义召回

`postgres.memory.repository.ts` 中：

- `semanticSql` 对无 embedding 记录返回 `NULL`；
- `ORDER BY score DESC` 在 PostgreSQL 中默认把 NULL 放前面；
- query + embedding 同时存在时仍用 ILIKE 做硬前置过滤，纯语义相关但没有关键词重合的记忆永远进不了候选集。

影响：未嵌入记录可能挤占 top-k；所谓 hybrid recall 实际退化成“词法命中集合内再向量排序”。

建议：对 semantic score 使用 `COALESCE` 或 `NULLS LAST`；采用 lexical/vector 两路候选 union 后再融合排序；增加同义改写和 NULL embedding 的 PostgreSQL 回归测试。

### P1-14：上传大小限制可通过省略 sizeBytes 绕过

`uploadPresignBodySchema` 把 `sizeBytes` 定义为 optional，`OssClient` 也只在该字段存在时校验。调用者省略大小即可获得 PUT URL；当前签名没有绑定 content-length 上限。

建议：presign 必须要求可信 `sizeBytes`，并在 OSS policy/回调层验证真实对象大小；文件名 public URL 也应逐段 URL encode，避免 `#`、`%`、空格导致不可用链接。

## 5. P2 问题

### P2-01：生产 Digital World 失败时会把 mock 数据伪装成真实数据

hook 初始 state 就是 mock。生产环境请求失败或返回空数据时，代码只把 `loaded=true, isMock=false` 合并到旧 state，没有清空 mock nodes/beings。结果是生产页面显示 mock 世界，却不显示 `DEV MOCK` 标识。

建议：生产失败态使用空数组和显式 error；mock 只在 DEV 分支创建，不能作为生产初始 state。

### P2-02：多数字生命选择后，唤醒/休眠仍操作 primary being

面板已使用 `activeBeing`，但 `DigitalWorld.tsx:77-90` 的 `handleWakeSleep()` 仍引用 `being`。选中其他数字生命后，dock 的唤醒/休眠会误操作第一个 being。

### P2-03：配置文件公开了多个不生效选项

`config.json` 中的 `server.port`、`security.requireLocalToken`、`skills.directories`、`skills.autoReload` 可以读写，但 daemon 的端口、token 开关和 SkillRegistry 并不从这些字段装配。API 返回成功会让使用者误以为配置已生效。

建议：要么接入运行时，要么从可写 schema 移除并标记只读/未支持。

### P2-04：Memory retry 发出协议外事件

`MemoryRetryWrapper` 通过 `as any` 发出 `agent.memory.write_failed`，但该 method 不在 `AGENT_EVENT_TYPES`；前端和协议消费者不会稳定识别。应统一使用 `agent.error` + memory category，或正式扩展协议并补 schema/consumer。

### P2-05：错误响应泄露内部异常文本

HTTP 全局 500、`/v1/chat` 和 JSON-RPC internal error 都直接返回 `Error.message`。数据库 SQL、上游 LLM response body、本机路径等信息可能暴露给客户端。

建议：客户端返回稳定 error code/request id，详细异常只写结构化日志并做 secret redaction。

### P2-06：限流器会永久保留访问过的 IP key

`SlidingWindowRateLimiter` 会裁剪单个 bucket 的时间戳，但从不删除空 bucket。若 daemon 对外暴露或经过大量伪造源地址，Map 会持续增长。

建议：空 bucket 删除、定期 sweep，并限制全局 key 数；生产部署优先使用成熟 rate-limit 插件/反向代理。

### P2-07：Embedding dimensions 配置与 provider/schema 不一致

环境变量允许修改 `SUNPILOT_EMBEDDING_DIMENSIONS`，但默认 embedding provider 没有把该值传入，数据库列固定为 `vector(1536)`。非 1536 设置会造成 service 声明、provider 返回和数据库 schema 三方不一致。

建议：若只支持 1536，就把 env 收紧为 literal 1536；若支持多维度，需要 migration/model config 的原子切换策略。

### P2-08：RichText 链接缺少 URL scheme allowlist

`RichTextRenderer` 和 `linkify()` 会直接把结构化 `href` 或 markdown link 写入 `<a href>`。应只允许 `https:`, `http:`, `mailto:` 等明确 scheme，拒绝 `javascript:`、`data:` 和畸形 URL；媒体 URL 同样需要策略。

### P2-09：Digital World 完成回调为 O(beings × actions) 扫描

`findActionByRunId()` 逐个读取 being，再逐个拉 action。2026-06-29 已恢复受版本控制的 `023_world_actions_agent_run_index.sql` 和 `idx_world_actions_agent_run`，但 repository 仍缺少 `findByAgentRunId()`，O(beings × actions) 的应用层扫描尚未修复。

## 6. 生产依赖审计

使用 npm 官方 registry 执行 `pnpm audit --prod`，当前报告 4 个生产依赖漏洞：

| 严重度 | 依赖路径 | Advisory | 备注 |
| --- | --- | --- | --- |
| High | `@fastify/static -> glob@11.0.0` | `GHSA-5j98-mcp5-4vw2` | glob CLI command injection；当前应用未直接使用 glob CLI，但仍应升级锁定版本 |
| Moderate | `@fastify/static@8.2.0` | `GHSA-pr96-94w5-mx2h` | directory listing path traversal |
| Moderate | `@fastify/static@8.2.0` | `GHSA-x428-ghpx-8j92` | encoded path separator route guard bypass |
| Moderate | `streamdown -> mermaid -> dompurify@3.4.10` | `GHSA-cmwh-pvxp-8882` | DOMPurify 配置污染 |

建议先验证 `@fastify/static >= 9.1.1` 与 Fastify 5 的兼容性，再升级；DOMPurify 应至少到 3.4.11。依赖审计必须加入 CI，并显式指定支持 audit endpoint 的 registry。

## 7. 逐包评价

| 包 | 当前优点 | 主要风险/缺口 |
| --- | --- | --- |
| `protocol` | 命令、事件、message parts、rich cards 集中管理 | 多数 event payload 仍靠断言；attachment-only 契约错误；包内无测试 |
| `storage` | repository 分层清楚，SQL 大多参数化，关键状态写入有事务；migration 已改为自动发现并通过空库验证 | memory 排序问题；nullable patch 语义不完整 |
| `skill-sdk` | capability 和测试 helper 简洁 | SDK 约束不能代表运行隔离 |
| `skill-runner` | manifest/path/host/env allowlist、审计、并发控制均有实现 | 同进程 import、协作式超时、symlink/redirect 绕过边界 |
| `core` | Agent Loop、stream、tool、批次审批、assistant 持久化失败语义、memory、trace 的模块结构完整；工具安全已收口到强制执行边界 | 第三方 Skill 仍缺少进程级隔离 |
| `platform` | Conversation/Digital World 与 Agent Core 解耦方向正确 | 约 1,099 行源码却没有测试；任务状态机、字段清空、并发 claim 均有生产 bug |
| `api` | REST/WS 适配层较薄，主要输入有 Zod | REST/WS chat 不对称；上传大小可绕过；内部错误泄露；除 OSS 外测试覆盖较薄 |
| `daemon` | 唯一 composition root、worker、recovery、metrics 清晰；生产工具路径已装配统一安全边界 | 模型配置仍分裂；Digital World bridge 类型漂移 |
| `web` | Chat 流式体验、附件、审批、debug、Digital World 功能丰富；类型检查已成为构建前置门禁 | 默认模型错误；生产 mock 伪装；测试有 React 警告；链接策略不足 |
| `launcher` | 命令面简单，已有基础测试 | PID 复用可误杀；进程发现依赖 substring |

## 8. 验证结果

| 检查 | 结果 | 说明 |
| --- | --- | --- |
| `pnpm typecheck` | 通过 | Web 3 个 TypeScript 错误已修复 |
| `pnpm lint` | 通过但有警告 | 6 个 React hooks warnings |
| `pnpm build` | 通过 | Web 构建现在强制先执行 `tsc --noEmit` |
| `pnpm test`（当前环境） | 通过 | 606 个测试通过；宿主机存在真实密钥时 fake provider 仍保持隔离 |
| `pnpm audit --prod` | 未复验 | 2026-06-28 原始结果为 1 high + 3 moderate，仍按未修复处理 |
| 空 PostgreSQL migration smoke | 通过 | 23 个 migration、12 个 Digital World 外键、`idx_world_actions_agent_run` 均已验证 |
| `git diff --check` | 通过 | 当前代码与文档无空白错误 |

测试通过仍不能覆盖本报告中的 Digital World 状态机风险：`packages/platform` 使用 `--passWithNoTests`，实际没有测试文件；集成测试仍只有一个 WebSocket happy path。多工具批次审批和工具安全已有 core 拒绝测试及 daemon 生产装配测试，clean-database migration 已做真实 PostgreSQL smoke；默认模型和 Digital World lifecycle 仍缺端到端门禁。

Web 测试虽然通过，但 stderr 已出现：

- `NaN` 被写入 CSS height；
- RecentConversations 缺少稳定 React key；
- 多个 Ant Design deprecated API 警告；
- 6 个 hooks dependency warning。

生产 bundle 中主应用 chunk 约 953 KB，Ant Design chunk 约 898 KB（gzip 各约 290 KB），Digital World/Pixi 与 rich rendering 后续应继续做路由级懒加载和 bundle budget。

## 9. 推荐修复顺序

### 第一批：发布阻断项

1. `[已完成]` 把 prompt injection、sandbox、task-scoped permission 接到不可绕过的工具执行入口；
2. `[待处理]` 明确 Skill 仅可信，或完成进程级隔离；
3. `[已完成]` 修复 Web typecheck，并让 Web build 必须运行 `tsc --noEmit`；
4. `[已完成]` 依赖注入测试与宿主机真实 model/embedding provider 隔离。

### 第二批：核心状态一致性

1. `[已完成]` 修复 assistant message 持久化失败语义；
2. `[已完成]` 统一动态风险审批和多工具批次审批；
3. `[待处理]` 修复 Digital Being nullable patch、异步 action 恢复和 artifact payload；
4. `[待处理]` 把 task claim 改为条件更新/行锁；
5. `[已完成]` 修复 migration 022/023 的仓库与数据库版本一致性。

### 第三批：契约与检索质量

1. 统一 attachment-only、REST/WS chat 和模型默认值；
2. 修复 memory hybrid candidate/NULL score；
3. 强制上传大小并治理 public URL；
4. 收敛 config schema、embedding dimensions 和事件词汇；
5. 升级有漏洞的生产依赖。

### 第四批：测试与可维护性

1. 为 `platform`、`protocol`、`api` 增加包级测试；
2. `[部分完成]` clean PostgreSQL migration、批次审批和工具 sandbox 生产装配已实测；并发 task 和第三方 Skill 进程隔离测试仍待补充；
3. 将 `composition-root.ts` 拆为 model/context/tool/safety/persistence 工厂，并对每个工厂做 wiring test；
4. 建立真实模型 eval，但与默认单元/集成测试严格隔离；
5. 清理 React warnings、deprecated API 和 bundle 体积。

---

### 第五批：Digital World 画布体验升级（2026-06-29 确定方案，待实施）

本节针对 Digital World 前端画布的三项体验缺陷：画布非无限、加载性能不稳定、视觉设计不够高级。

#### 问题一：画布不是无限画布

**根因**：`WorldApp.drawWorld()` 中网格和背景都是一次性绘制固定尺寸（网格 `max(viewport, 3000)` × `max(viewport, 2000)`；背景 `computeWorldBounds() + 300px`），拖拽超出即白屏。

**方案：视口跟随分块渲染**

| 改动点 | 文件 | 说明 |
|--------|------|------|
| 网格改为视口跟随 | `canvas/WorldGrid.ts` | `draw()` 接收 `cameraBounds: { minX, minY, maxX, maxY }` 而非固定 width/height，仅绘制可见区域 + 一圈 buffer |
| 相机移动触发网格刷新 | `canvas/CameraController.ts` | `handlePointerMove` 末尾调用 `redrawGrid()`，每次拖拽都重算可见网格范围 |
| 背景解耦 | `canvas/WorldApp.ts` | 去掉 PixiJS 层的 `FillGradient` 大矩形，改用 `app.renderer.background.color` 纯色兜底 + CSS `background` 在 canvas 宿主下方铺一层 |
| 无隐式边界 | `CameraController.ts` | 当前本就无 clamp，唯一阻碍是固定尺寸网格。改为动态网格即实现真无限拖拽 |

#### 问题二：加载性能 / 画布不显示

**根因**：

- **初始化竞态**：`useWorldApp` 的 `mount()`（含 `app.init()`）是异步的，而 `setData` 的 effect 检查 `mountedRef.current`。如果 bootstrap 数据先到达但 PixiJS 尚未 init 完成，`setData` 直接 return 丢弃数据，只能等待下次 polling
- **PixiJS init 慢**：WebGL 上下文创建 + shader 编译，期间 canvas 完全空白
- **布局尺寸为 0**：父容器在 mount 时可能 flexbox 尚未计算出最终尺寸

**方案**：

| 改动点 | 文件 | 说明 |
|--------|------|------|
| 数据排队 | `hooks/useWorldApp.ts` | 新增 `pendingDataRef`，mount 未完成时暂存数据，`mount().then()` 后自动 apply |
| 加载态 UI | `DigitalWorld.tsx` + `.scss` | `loaded === false` 时在 canvas 宿主上覆盖 skeleton/spinner |
| 显式首帧 | `canvas/WorldApp.ts` | `app.init()` 后立即 `app.render()`，不等待 ticker |
| 延迟非关键层 | `canvas/WorldApp.ts` | ParticleLayer、SoundManager 延后到首帧渲染后启动 |
| 尺寸兜底 | `hooks/useWorldApp.ts` | ResizeObserver 初始化时若尺寸为 0，用 `requestAnimationFrame` retry |

#### 问题三：视觉设计不够高级

**方向**：从"卡通小镇"升级为 **"数据指挥中心 / Control Room"**（类似 Grafana + Tron 的结合体），默认深色主题。

**3.1 新增 CONTROL_ROOM 主题（`constants.ts`）**

| Token | 当前 Light | 新 Control Room |
|-------|-----------|-------------------|
| `canvasBg` | `#ffffff` | `#0a0e17` (深空蓝黑) |
| `canvasBgGradientTop` | `#f0f4ff` | `#0d1321` |
| `canvasBgGradientBottom` | `#ffffff` | `#0f1729` |
| `gridColor` | `#c7d2fe` | `#1a2744` α=0.4 |
| `roadColor` | `#d1d5db` | `#1e3050` |
| `roadCenterColor` | `#fbbf24` (琥珀) | `#06b6d4` (cyan) |
| `nodeBg` | `#ffffff` | `#141c2b` |
| `nodeBorder` | `#e5e7eb` | `#1e3a5f` |
| `textColor` | `#1f2937` | `#c8d6e5` |

保留 `LIGHT_THEME` 供切换，`CONTROL_ROOM` 设为默认。

**3.2 网格**（`canvas/WorldGrid.ts`）
- 圆点 → 细线交叉网格（0.5px 线宽，alpha 0.15），每 5 格一条稍亮二级线
- 移动时微弱视差（网格移动速度 ×0.92 相机速度）

**3.3 节点**（`canvas/WorkstationNode.ts`）
- 圆角矩形卡片 → 六边形（`regularPolygon` 6 边），更有拓扑图感
- 状态指示：右上角小圆点 → 整圈发光线（active 时呼吸光晕）
- 新增：节点下方小型数据条形指示器
- 连线交汇处绘制发光圆点

**3.4 道路**（`canvas/RoadLayer.ts` + `canvas/ParticleLayer.ts`）
- 双线+虚线 → 单线 + 流动粒子
- `ParticleLayer` 增强：沿道路流动的数据包（发光圆点沿 edge 匀速移动）
- 道路按类型分色：cyan = 数据流，purple = 产物流，amber = 控制流

**3.5 数字生命**（`canvas/DigitalBeingEntity.ts`）
- 当前：履带机器人 + 眨眼 + 胳膊（Q 版可爱风）
- 改为：**几何核心 + 数据环**
  - 核心：旋转多面体线框（二十面体），悬浮在节点上方
  - 环：围绕核心的轨道环（原子模型风格），环上流动光点
  - idle → 慢旋转 + 白色核心
  - working → 快旋转 + cyan + 粒子加速
  - sleeping → 静止 + 暗淡
  - error → 红色闪烁
  - moving → 沿道路滑动 + 拖尾粒子
- 去掉眼睛、胳膊等拟人元素

**3.6 环境粒子**（`canvas/ParticleLayer.ts`）
- 全屏覆盖的微细缓慢上升粒子（极稀疏、低 alpha），增强"数据空间"沉浸感

**3.7 排版**
- 节点标题：13px, `font-weight: 600`, `letter-spacing: 0.5px`
- 数据指标：11px monospace（`JetBrains Mono` / `Fira Code`）
- 文字阴影确保深色背景可读性

**3.8 实施顺序**

| 阶段 | 内容 | 文件范围 | 改动量 |
|------|------|----------|--------|
| 一 | 无限画布 + 加载性能（问题一、二） | `useWorldApp.ts`, `WorldGrid.ts`, `CameraController.ts`, `WorldApp.ts` | 小～中 |
| 二 | 新主题 + 网格升级 + 道路粒子 | `constants.ts`, `WorldGrid.ts`, `RoadLayer.ts`, `ParticleLayer.ts` | 中 |
| 三 | 节点六边形重设计 | `WorkstationNode.ts` | 中 |
| 四 | 数字生命几何体 + 环境粒子 | `DigitalBeingEntity.ts`, `ParticleLayer.ts` | 大 |

一、二阶段可并行（不同文件），三、四依赖二的设计 token。

## 10. 最终判断

架构方向仍然成立。2026-06-29 已完成工具安全边界、类型门禁、测试 provider 隔离、fast-ack 清理、assistant 持久化失败语义、动态/批次审批以及 migration 022/023 收口；这些问题不再作为当前阻断项。

剩余的主要落差仍在两个领域：工具调用层已有统一安全门，但第三方 Skill 仍在 daemon 进程内动态加载，尚未形成真正的执行隔离；Digital World 已跨过 UI mock 阶段，但任务状态机和 PostgreSQL nullable patch 语义尚未形成可靠闭环。

因此，本轮建议不是继续增加新模块，而是先完成三项收口：

1. 工具安全执行门已完成，下一步把第三方 Skill 隔离变成进程级真实边界；
2. 继续完成 Digital World 任务和数据状态机；消息、审批和 migration 已完成本轮收口；
3. typecheck、无外网单元测试、工具安全和空库 migration 已形成门禁，下一步补齐 Skill 隔离与 Digital World 回归。

完成这些工作后，SunPilot 才适合从“功能完整的 Agent Runtime 开发版”进入“可持续发布的生产候选版”。
